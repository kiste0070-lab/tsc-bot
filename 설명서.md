# 하루 1문장 중국어 암기 봇 (HSK 4급)

매일 **HSK 4급 수준**의 실용 중국어 문장 **1개**를 외우는 텔레그램 봇입니다.  
TTS 음성, Gemini 피드백, GitHub Actions 자동 실행을 지원합니다.

---

### 1. 주요 기능

* **하루 1문장 (HSK 4급)**: `Daily_Sentences/YYYY_MM.md`에 월별로 문장이 저장됩니다.
* **연간 선생성 (12개월)**: 시작 월부터 **1년치(12개월)** 문장을 미리 생성합니다. 기간 정보는 `Daily_Sentences/plan_anchor.json`에 저장됩니다.
* **HSK 4급 맞춤**: Gemini가 HSK 4급 어휘·문법(把/被, 不但…而且, 虽然…但是 등)을 반영한 문장을 생성합니다.
* **음성 학습**: 오늘의 문장을 TTS(`zh-CN`)로 들을 수 있습니다.
* **대화형 학습**: 문장설명, 예문보기, 따라말하기, 학습종료 명령 지원
* **학습 기록**: `study_notes/YYYYMM_study_notes.md`에 피드백이 누적됩니다.
* **GitHub Actions**: 매일 한국 시간 11:00에 자동 실행

### 2. 설치 및 환경 설정

#### 필수 요구 사항
* **Python 3.10 이상** 권장
* 필수 라이브러리:
  ```bash
  pip install gTTS python-telegram-bot google-genai python-dotenv
  ```

#### 환경 변수 (`.env`)
```env
TELEGRAM_BOT_TOKEN=여러분의_텔레그램_봇_토큰
GEMINI_API_KEY=여러분의_제미나이_API_키
CHAT_ID=여러분의_텔레그램_ID
```

선택 사항:
```env
# GEMINI_MODEL_PRIMARY=gemini-2.5-flash
# GEMINI_MODEL_SECONDARY=gemini-2.5-flash-lite
```

#### GitHub Actions Secrets
| Secret | 설명 |
|---|---|
| `TELEGRAM_BOT_TOKEN` | 텔레그램 봇 토큰 |
| `GEMINI_API_KEY` | Gemini API 키 |
| `CHAT_ID` | 텔레그램 채팅 ID |

> Workflow permissions: **Read and write permissions** 필요 (학습 기록·문장 계획 자동 커밋)

### 3. 폴더 구조

```
Daily_Sentences/
  plan_anchor.json     ← 연간 계획 기준 (시작월~12개월)
  2026_07.md           ← 2026년 7월 문장
  2026_08.md
  ...
  2027_06.md           ← 시작이 7월이면 12개월 후 6월까지

study_notes/
  202607_study_notes.md

sentence_plan.py       ← 문장 생성·조회 로직
generate_yearly_sentences.py  ← 1년치 일괄 생성 스크립트
main.py                ← 텔레그램 봇
```

### 4. Daily_Sentences 형식

```markdown
### 2026-07-06
문장 : 虽然今天很累，但是我还是要把作业做完。
주제 : 의지·계획 표현
메모 : 피곤해도 해야 할 일을 마무리할 때
```

문장 난이도는 **HSK 4급** 기준입니다.

### 5. 연간 문장 미리 생성

이번 달부터 12개월치 문장을 한 번에 만들 때:

```bash
python generate_yearly_sentences.py
```

특정 월부터 시작:
```bash
python generate_yearly_sentences.py --year 2026 --month 7
```

기존 파일 덮어쓰기:
```bash
python generate_yearly_sentences.py --force
```

봇 실행 시에도 `plan_anchor.json` 기준으로 빠진 월이 있으면 자동 생성합니다.

### 6. 사용 방법

1. 봇이 오늘의 문장 + 병음 + 한국어 뜻 + 음성을 보냅니다.
2. 명령어:
   - `문장설명` — 문법·어휘·발음 설명
   - `예문보기` — HSK 4급 활용 예문
   - `따라말하기` — 따라 쓰기 안내
   - 중국어로 문장 입력 — 첨삭 피드백
   - `학습종료` — 요약 후 종료

### 7. 실행

**로컬**
```bash
python main.py
```

**GitHub Actions**  
매일 KST 11:00 자동 실행. 수동 실행: Actions → `Daily Chinese Sentence` → Run workflow

### 8. 문제 해결

* **문장을 찾을 수 없음**: `python generate_yearly_sentences.py`로 연간 계획 재생성
* **Gemini API 에러**: 재시도 후 보조 모델로 폴백. `GEMINI_MODEL_*` 환경 변수 확인
* **gTTS 에러**: 인터넷 연결 확인

### 9. 학습 팁

1. 음성을 먼저 듣고 쉐도잉한 뒤 텍스트를 확인하세요.
2. `문장설명`으로 把/被 등 HSK 4급 문법 포인트를 익히세요.
3. `study_notes`를 주기적으로 복습하세요.
4. 연간 계획이 끝나면 `generate_yearly_sentences.py`로 다음 1년치를 생성하세요.

### 10. 자동 생성 프롬프트 (엄격한 형식)

아래 프롬프트는 연간·월별 문장 자동 생성 시 사용하는 엄격한 출력 규칙입니다. 모델이 한 글자도 어기지 않도록 입력하세요.

```
2026년 한 해(윤년 아님) 분량의 '매일 문장' 총 365개를 HSK 4급 수준 문장으로 생성하시오. 출력 형식은 아래 규칙을 엄격히 지키고, 불필요한 설명이나 해설은 절대 추가하지 마시오.

1) 출력 단위: 날짜별 항목(예: 2026-01-01)으로 하되, 월별로 묶어 출력하되 파일 분할은 하지 않고 연속 텍스트로 출력하시오.
2) 각 항목 구조(정확히 2줄, 그 다음에 빈 줄):
   - 1행: 한자(병음, 한글 뜻)
     예: 你好(nǐ hǎo, 안녕하세요)
   - 2행: 외워야 하는 '단어/숙어/문장 형식'을 반드시 소개
     형식: 외워야 할 항목: [단어1 — 품사(한글 뜻)], [숙어 — 뜻], [문장형식 — 설명]
     예: 외워야 할 항목: 你(nǐ) — 대명사(너), 好(hǎo) — 형용사(좋다), 인사 표현 — 간단 인사
3) 항목 사이에는 반드시 한 줄의 빈 줄을 넣어 구분하시오.
4) 날짜 라벨은 ISO 포맷(YYYY-MM-DD)로 표기하고, 2026-01-01부터 2026-12-31까지 빠짐없이 순서대로 생성하시오.
5) 문장 수준과 내용: 모든 문장은 HSK 4급 수준 어휘와 문법을 사용하고, HSK 또는 TSC 시험에서 빈번하게 나오는 문장 형식(예: 비교, 가능한/불가능 표현, 정도 보어, 결과 보어, 连...都/也, 把자문, 被자문, 虽然...但是 등)을 포함하시오. 병음은 반드시 성조를 포함하여 표기하시오(예: nǐ hǎo).
6) 각 항목의 '한자'는 시험에 적합한 예문으로 하시오.
7) 출력 예외, 주석, 추가 설명, 번호표, 마크다운 코드블록, 테이블 등은 일절 사용하지 마시오.
8) 출력이 끝나면 '완료'나 다른 문구를 추가하지 말고 마지막 항목 다음에는 빈 줄 하나로 끝내시오.

예시(정확한 포맷):
2026-01-01
你好(nǐ hǎo, 안녕하세요)
외워야 할 항목: 你(nǐ) — 대명사(너), 好(hǎo) — 형용사(좋다), 인사 표현 — 간단 인사

위 규칙을 한 글자도 어기지 말고 365개 항목을 HSK 4급 수준으로 생성하시오.
```
