# 하루 1문장 중국어 암기 봇 (HSK 4급)

매일 **HSK 4급 수준**의 실용 중국어 문장 **1개**를 외우는 텔레그램 봇입니다.  
TTS 음성, Gemini 기반 자동 생성, GitHub Actions 1회성 자동 전달을 지원합니다.

---

### 1. 주요 기능

* **하루 1문장 (HSK 4급)**: `Daily_Sentences/YYYY_MM.md`에 월별로 문장이 저장됩니다.
* **반년치 선생성 (6개월)**: 시작 월부터 **반년치(6개월)** 문장을 미리 생성합니다. 기간 정보는 `Daily_Sentences/plan_anchor.json`에 저장됩니다.
* **HSK 4급 맞춤**: Gemini가 HSK 4급 어휘·문법(把/被, 不但…而且, 虽然…하지만 등)을 반영한 문장을 생성합니다.
* **음성 학습**: 오늘의 문장을 TTS(`zh-CN`)로 들을 수 있습니다.
* **1회성 메시지 자동 전달**: 매일 한국 시간 11:00에 텔레그램으로 오늘의 문장과 음성을 전달합니다.

### 2. 설치 및 환경 설정

#### 필수 요구 사항
* **Python 3.10 이상** 권장
* 필수 라이브러리:
  ```bash
  pip install gTTS python-telegram-bot google-genai python-dotenv
  ```

#### 환경 변수 (`.env`)
```env
TELEGRAM_BOT_TOKEN=여러분의_텔레그램_봇_토큰
GEMINI_API_KEY=여러분의_제미나이_API_키
CHAT_ID=여러분의_텔레그램_ID
```

선택 사항:
```env
# GEMINI_MODEL_PRIMARY=gemini-2.5-flash
# GEMINI_MODEL_SECONDARY=gemini-2.5-flash-lite
```

#### GitHub Actions Secrets
| Secret | 설명 |
|---|---|
| `TELEGRAM_BOT_TOKEN` | 텔레그램 봇 토큰 |
| `GEMINI_API_KEY` | Gemini API 키 |
| `CHAT_ID` | 텔레그램 채팅 ID |

> Workflow permissions: **Read and write permissions** 필요 (학습 기록·문장 계획 자동 커밋)

### 3. 폴더 구조

```
Daily_Sentences/
  plan_anchor.json     ← 6개월 계획 기준 (시작월~6개월)
  2026_07.md           ← 2026년 7월 문장
  2026_08.md
  ...

study_notes/
  202607_study_notes.md

text_utils.py          ← 텍스트 처리 유틸리티
sentence_plan.py       ← 문장 생성·조회 로직
generate_yearly_sentences.py  ← 6개월치 일괄 생성 스크립트
main.py                ← 텔레그램 봇

tests/                 ← 단위 테스트
```

### 4. 6개월 문장 미리 생성

이번 달부터 6개월치 문장을 한 번에 만들 때:

```bash
python generate_yearly_sentences.py
```

특정 월부터 시작:
```bash
python generate_yearly_sentences.py --year 2026 --month 7
```

기존 파일 덮어쓰기:
```bash
python generate_yearly_sentences.py --force
```

봇 실행 시에도 `plan_anchor.json` 기준으로 빠진 월이 있으면 자동 생성합니다.

### 5. 사용 방법

1. 매일 KST 11:00에 봇이 오늘의 문장(한자, 병음, 한국어 뜻, 넘버링 및 (pinyin) 포함 외워야 할 항목) + TTS 음성을 텔레그램 메시지로 1회성 발송합니다.

### 6. 실행

**로컬**
```bash
python main.py
```

**GitHub Actions**  
매일 KST 11:00 자동 실행. 수동 실행: Actions → `Daily Chinese Sentence` → Run workflow

### 7. 문제 해결

* **문장을 찾을 수 없음**: `python generate_yearly_sentences.py`로 6개월 계획 재생성
* **Gemini API 에러**: 재시도 후 보조 모델로 폴백. `GEMINI_MODEL_*` 환경 변수 확인
* **gTTS 에러**: 인터넷 연결 확인

### 8. 학습 팁

1. 음성을 먼저 듣고 쉐도잉한 뒤 텍스트를 확인하세요.
2. `study_notes` 또는 `Daily_Sentences`를 주기적으로 복습하세요.
3. 6개월 계획이 끝나면 `generate_yearly_sentences.py`로 다음 6개월치를 생성하세요.

### 9. 자동 생성 프롬프트 (엄격한 형식)

아래 프롬프트는 **월별** 문장 자동 생성 시 `sentence_plan.py`가 실제로 사용하는 엄격한 출력 규칙입니다. 한 달치(해당 월 전체 일수)를 한 번에 생성합니다.

```
너는 중국어 회화 학습 콘텐츠 제작 전문가야.
{year}년 {month}월의 '하루 1문장' 학습 계획을 만들어줘.

[요청]
- {year}년 {month}월 1일~{num_days}일, 매일 실생활에서 자주 쓰이는 중국어 문장 1개씩 (총 {num_days}개)
- **HSK 4급 수준** 문장 (어휘·문법 모두 HSK 4 범위, 약 10~20자)
- HSK 4급 문법 활용 권장: 把/被, 不但…而且, 虽然…但是, 越…越…, 连…都, 결과보어 등
- HSK 4급 어휘 위주, 1~3급만으로 된 너무 단순한 문장은 피할 것
- 직장, 여행, 건강, 취미, 사회생활, 감정·의견 표현 등 HSK 4에 맞는 주제
- 같은 달·기존 문장과 절대 중복 금지

[출력 형식 - 이 형식만 사용]
### YYYY-MM-DD
중국어한자문장
(pinyin)
한국어번역
외워야 할 항목:
1. 어휘(pinyin) — 품사(한글 뜻)
2. 어휘(pinyin) — 품사(한글 뜻)
3. 문장형식 — 설명

[규칙 - 엄격 준수]
1. 각 날짜 아래에는 아래 순서대로 정확히 작성합니다:
   - 1행: 중국어 한자 문장만
   - 2행: 성조를 포함한 병음만 괄호 안에 표기 (예: (Nǐ hǎo.))
   - 3행: 한글 번역 뜻만 (예: 안녕하세요.)
   - 4행: "외워야 할 항목:" 헤더
   - 5행부터: 넘버링(1., 2., 3.)으로 1개 항목당 1줄씩 작성하되, 어휘 한자 옆에 반드시 (pinyin)을 표기 (예: 1. 你(nǐ) — 대명사(너))
2. 모든 문장은 HSK 4급 학습자가 이해·암기하기 적합한 난이도
3. {year}년 {month}월 모든 날짜를 빠짐없이 포함
4. 설명·인사말 없이 위 형식만 출력
5. 날짜 헤더는 ### YYYY-MM-DD 형식
```
